#include "main.h"

/**
 * swap_int - swaps the values of two integers
 * @a: integer to swap
 * @b: integer to swap
 * return 0
 */

int _strlen(char *s);
{
  int count = 0;

	while (*(s + count) != '\0')
		count++;
  
	return (count);
}
